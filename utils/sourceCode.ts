export const projectFiles: Record<string, string> = {
    // This functionality has been replaced by a direct link to the GitHub repository.
    // The object is kept to prevent import errors in older components that may still reference it,
    // but it should no longer be used.
};